// import fetchPhotos from '../stores/photos/actions'
// import { RECEIVE_PHOTOS } from '../stores/photos/actionTypes';
// import fetchPhotosJson from '../api/photosApi';

// describe('actions', () => {
//   it('should create an action to get photos', () => {
//     const expectedAction = {
//       type: RECEIVE_PHOTOS,
//       photos: {}
//     }
//     expect(fetchPhotos()).toEqual( function(dispatch) {
//         return fetchPhotosJson()
//           .then(json => dispatch(receivePhotos(json)))
//       });
//   })
// })

test('test', ()=>{
    expect(1+1).toBe(2);
})